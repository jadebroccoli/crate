{
  "name": "@crate/desktop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "clean": "rm -rf src-tauri/target"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.2.0"
  }
}
